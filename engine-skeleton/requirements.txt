fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
# AWS connectivity — S3 CUR pointer fetch + DynamoDB idempotency (lazy-imported;
# only needed in deployment / for S3_POINTER. Local/offline uses S3_LOCAL_DIR).
boto3>=1.34.0
# Optional: ML overlay model (AI_OVERLAY_MODEL). Skip if running statistical-only.
# scikit-learn>=1.4.0
# joblib>=1.3.0
