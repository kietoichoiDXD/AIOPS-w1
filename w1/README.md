# AIOps Week 1

Repo is split by day to match the submission convention:

- `d1/` for Metric Anomaly Detection
- `d2/` for Log Mining + Parsing + Anomaly from Log

Each day folder should contain its own notebook, submission note, and any outputs or assets that belong to that day.
