fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
# Core runtime deps — imported at startup by statistical_detect_service.py,
# feature_builder.py, anomaly_classifier.py. REQUIRED in the Docker image
# (without these the container crashes on import: ModuleNotFoundError: numpy).
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
# AWS connectivity — S3 CUR pointer fetch + DynamoDB idempotency + Bedrock RCA
# (boto3 imported at startup by decision_service/llm_rca).
boto3>=1.34.0
# Optional: ML overlay model (AI_OVERLAY_MODEL). Skip if running statistical-only.
# scikit-learn>=1.4.0
# joblib>=1.3.0
